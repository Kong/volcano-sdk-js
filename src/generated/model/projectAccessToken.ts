/**
 * Generated by orval 🍺
 * Do not edit manually.
 * Volcano Hosting API
 * Public API for Volcano Hosting clients, SDKs, and CLI tooling (Port 8000).
 *
 * This specification intentionally excludes first-party/internal APIs. See api/openapi-internal.yaml for non-public internal and Builder operations.
 * OpenAPI spec version: 3.0.0
 */
import type { ProjectAccessTokenScope } from './projectAccessTokenScope';
import type { ProjectAccessTokenStatus } from './projectAccessTokenStatus';
import type { ProjectAccessTokenTokenSource } from './projectAccessTokenTokenSource';

/**
 * A project access token: a control-plane credential bound to a single
 * project. Unlike a platform token, which acts on every project its owner
 * has, this one is limited to the project it was created in.
 *
 * The secret itself is never returned here. Only its hash is stored, so
 * the plaintext exists solely in the response to the create call.
 */
export interface ProjectAccessToken {
  id: string;
  project_id: string;
  /** Unique per project. */
  name: string;
  /** First 12 characters of the secret, for recognising a token in a list. */
  token_prefix: string;
  scope: ProjectAccessTokenScope;
  /**
     * `revoked` means the token was deliberately revoked, by you or by the
     * deletion of its project. `expired` means it simply reached
     * `expires_at`; nothing was taken away. Both are refused, and both keep
     * their record so a token's name, prefix, last use, and request history
     * remain available after a leak.
     *
     * A token revoked before its expiry passed stays `revoked`, because
     * that is the fact worth keeping.
     */
  status: ProjectAccessTokenStatus;
  /** What created the token. */
  token_source: ProjectAccessTokenTokenSource;
  /**
     * Absent for a token that does not expire.
     * @nullable
     */
  expires_at?: string | null;
  /**
     * Updated at most once every few minutes, so it may lag slightly.
     * @nullable
     */
  last_used_at?: string | null;
  created_at: string;
  /** Requests authenticated with this token since it was created. */
  all_time_requests: number;
}
